/**
 * Returns the `db.operation.name` value for a given tedious method.
 *
 * - `callProcedure` → `"EXECUTE"`
 * - `execBulkLoad`  → `"BULK INSERT"`
 * - everything else → the method name as-is (e.g. `"execSql"`, `"prepare"`)
 */
export declare function getOperationName(tediousMethod: string): string;
/**
 *   {db.operation.name} {target}
 *   {db.operation.name}
 *   {target}
 *   {db.system.name}
 *
 * where {target} resolves to db.collection.name first, then db.namespace.
 */
export declare function getSpanName(operationName: string | undefined, db: string | undefined, collection: string | undefined, dbSystemName: string): string;
export declare const once: (fn: Function) => (...args: unknown[]) => any;
//# sourceMappingURL=utils.d.ts.map