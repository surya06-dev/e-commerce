"use strict";
/*
 * Copyright The OpenTelemetry Authors
 * SPDX-License-Identifier: Apache-2.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTracerProviderFromConfig = exports.createIdGeneratorFromConfig = exports.createSamplerFromConfig = exports.createMeterProviderFromConfig = exports.createPushMetricExporterFromConfig = exports.createSpanLimitsFromConfig = exports.createLoggerProviderFromConfig = exports.createLogRecordProcessorFromConfig = exports.createLogRecordExporterFromConfig = exports.createLogRecordLimitsFromConfig = exports.createResourceFromConfig = exports.createPropagatorFromConfig = void 0;
/**
 * Create SDK components from parsed declarative config.
 * https://opentelemetry.io/docs/specs/otel/configuration/sdk/#create
 *
 * Dev Notes:
 * This file exports `create<SDK Thing>FromConfig(...)` functions intended to
 * be used by the "create" step of `startNodeSDK()`.
 */
const util_1 = require("util");
const fs_1 = require("fs");
const path = require("path");
const api_1 = require("@opentelemetry/api");
const sdk_trace_1 = require("@opentelemetry/sdk-trace");
const resources_1 = require("@opentelemetry/resources");
const exporter_logs_otlp_http_1 = require("@opentelemetry/exporter-logs-otlp-http");
const exporter_logs_otlp_grpc_1 = require("@opentelemetry/exporter-logs-otlp-grpc");
const exporter_logs_otlp_proto_1 = require("@opentelemetry/exporter-logs-otlp-proto");
const exporter_metrics_otlp_grpc_1 = require("@opentelemetry/exporter-metrics-otlp-grpc");
const exporter_metrics_otlp_http_1 = require("@opentelemetry/exporter-metrics-otlp-http");
const exporter_metrics_otlp_proto_1 = require("@opentelemetry/exporter-metrics-otlp-proto");
const exporter_trace_otlp_proto_1 = require("@opentelemetry/exporter-trace-otlp-proto");
const exporter_trace_otlp_http_1 = require("@opentelemetry/exporter-trace-otlp-http");
const exporter_trace_otlp_grpc_1 = require("@opentelemetry/exporter-trace-otlp-grpc");
const otlp_exporter_base_1 = require("@opentelemetry/otlp-exporter-base");
const otlp_grpc_exporter_base_1 = require("@opentelemetry/otlp-grpc-exporter-base");
const exporter_metrics_otlp_http_2 = require("@opentelemetry/exporter-metrics-otlp-http");
const configuration_1 = require("@opentelemetry/configuration");
const sdk_logs_1 = require("@opentelemetry/sdk-logs");
const propagator_jaeger_1 = require("@opentelemetry/propagator-jaeger");
const propagator_b3_1 = require("@opentelemetry/propagator-b3");
const core_1 = require("@opentelemetry/core");
const sdk_metrics_1 = require("@opentelemetry/sdk-metrics");
const exporter_prometheus_1 = require("@opentelemetry/exporter-prometheus");
const semantic_conventions_1 = require("@opentelemetry/semantic-conventions");
// ---- internal utilities
function getGrpcMetadataFromHeaders(headers, headersList) {
    const headerValues = (0, configuration_1.mergeHeadersConfig)(headers, headersList);
    if (!headerValues || Object.keys(headerValues).length === 0) {
        return undefined;
    }
    const metadata = (0, otlp_grpc_exporter_base_1.createEmptyMetadata)();
    for (const [name, value] of Object.entries(headerValues)) {
        metadata.set(name, value);
    }
    return metadata;
}
/**
 * Warn if some props from a declarative config object have not been handled.
 *
 * This is intended to be used by `create*FromConfig()` functions. It is a low
 * tech mechanism to add awareness when a given valid config is not being
 * completely handled. This could help when properties are added to the
 * configuration schema. (A higher tech mechanism that wraps the parsed
 * configuration during `create()` and watches for untouched properties
 * might be nice.)
 */
function checkConfigUse(name, props, handledProps) {
    if (!props)
        return;
    // Dev note: I'd use Set#difference, but that requires Node.js v22.
    const unhandledProps = Object.keys(props).filter(k => !handledProps.includes(k));
    if (unhandledProps.length > 0) {
        api_1.diag.warn(`Config warning: some specified ${name} configuration properties were not handled by SDK setup: ${JSON.stringify(unhandledProps)}`);
    }
}
/**
 * Return the single non-undefined entry in the given config object, or throw.
 *
 * It is common for Declarative Configuration to have config objects with
 * a single entry, e.g.
 *
 *    "LogRecordProcessor": {
 *      "type": "object",
 *      "additionalProperties": {
 *        "type": [
 *          "object",
 *          "null"
 *        ]
 *      },
 *      "minProperties": 1,
 *      "maxProperties": 1,
 *
 * The TypeScript types cannot express the minProperties/maxProperties from the
 * JSON schema. We guard against that here.
 */
function mustSingleEntry(configObj, configTypeName) {
    const entries = Object.entries(configObj).filter(([_name, properties]) => properties !== undefined);
    if (entries.length !== 1) {
        const entryNames = entries.map(e => e[0]);
        throw new Error(`invalid ${configTypeName} in configuration: must have exactly one entry: entries=${JSON.stringify(entryNames)}`);
    }
    return entries[0];
}
function loadTlsConfigFile(absPath, propName) {
    try {
        if (!path.isAbsolute(absPath)) {
            throw new Error(`TLS config file "${absPath}" must be an absolute path`);
        }
        return (0, fs_1.readFileSync)(absPath);
    }
    catch (err) {
        throw new Error(`could not load "tls.${propName}" from config: ${err.message}`, { cause: err });
    }
}
/**
 * Get TLS-related HTTP client options from declarative configuration.
 * https://opentelemetry.io/docs/specs/otel-config/types/#type-httptls
 * https://nodejs.org/api/tls.html#tlscreatesecurecontextoptions
 */
function httpTlsOptionsFromConfig(tls) {
    if (!tls) {
        return undefined;
    }
    checkConfigUse('HttpTls', tls, ['ca_file', 'cert_file', 'key_file']);
    const httpTlsOptions = {};
    if (tls.ca_file) {
        httpTlsOptions.ca = loadTlsConfigFile(tls.ca_file, 'ca_file');
    }
    if (tls.cert_file) {
        httpTlsOptions.cert = loadTlsConfigFile(tls.cert_file, 'cert_file');
    }
    if (tls.key_file) {
        httpTlsOptions.key = loadTlsConfigFile(tls.key_file, 'key_file');
    }
    return httpTlsOptions;
}
function grpcCredentialsFromConfig(tls) {
    if (!tls) {
        return undefined;
    }
    checkConfigUse('GrpcTls', tls, [
        'insecure',
        'ca_file',
        'cert_file',
        'key_file',
    ]);
    if (tls?.insecure) {
        return (0, otlp_grpc_exporter_base_1.createInsecureCredentials)();
    }
    const rootCert = tls.ca_file
        ? loadTlsConfigFile(tls.ca_file, 'ca_file')
        : undefined;
    const privateKey = tls.key_file
        ? loadTlsConfigFile(tls.key_file, 'key_file')
        : undefined;
    const certChain = tls.cert_file
        ? loadTlsConfigFile(tls.cert_file, 'cert_file')
        : undefined;
    if (rootCert || privateKey || certChain) {
        return (0, otlp_grpc_exporter_base_1.createSslCredentials)(rootCert, privateKey, certChain);
    }
    return undefined;
}
/**
 * Validate an export(er) timeout value.
 * "timeout" properties in the declarative config schema are typically
 * described with:
 *
 * 1. Value must be non-negative.
 * 2. A value of 0 indicates no limit (infinity).
 *
 * Search "timeout" at https://opentelemetry.io/docs/specs/otel-config/types/
 *
 * This function enforces (1), and guards against (2) because current OTel
 * JS SDK components do *not* handle a `0` timeout value.
 * See https://github.com/open-telemetry/opentelemetry-js/issues/6617
 */
function validateExportTimeoutConfig(timeout, errLabel) {
    if (timeout == null) {
        return undefined;
    }
    else if (timeout < 0) {
        throw new Error(`${errLabel} value must be non-negative: ${timeout}`);
    }
    else if (timeout === 0) {
        throw new Error(`${errLabel} of 0 (infinite) is not supported`);
    }
    return timeout;
}
// ---- create<SDKThing>FromConfig functions
function createPropagatorFromConfig(propagatorConfig) {
    const configComposite = (0, configuration_1.mergePropagatorCompositeConfig)(propagatorConfig.composite, propagatorConfig.composite_list);
    if (!configComposite) {
        return undefined;
    }
    // TextMapPropagator config items are objects with a single key (the name).
    // Transform this into a more convenient `(name, value)` 2-tuple.
    //
    // As well, guard against two cases where the TypeScript type
    // `TextMapPropagatorConfigModel` does not exactly represent the JSON schema:
    // 1. `"minProperties": 1, "maxProperties": 1,`
    // 2. The type allows keys with an `undefined` value, but the JSON schema
    //    does not.
    const kvFromItem = (item) => {
        const keys = [];
        let value = undefined;
        for (const key of Object.keys(item)) {
            value = item[key];
            if (value === undefined) {
                continue;
            }
            keys.push(key);
        }
        if (keys.length !== 1) {
            throw new Error(`invalid "propagator" entry in configuration, there must be exactly one key (with a non-undefined value): ${(0, util_1.inspect)(item)}`);
        }
        return [keys[0], value];
    };
    // First pass: handle 'none', remove dupes.
    const names = new Set();
    const kvs = [];
    for (const item of configComposite) {
        const kv = kvFromItem(item);
        const k = kv[0];
        if (names.has(k)) {
            continue;
        }
        names.add(k);
        kvs.push(kv);
        if (k === 'none') {
            return undefined;
        }
    }
    // Implementation note: this only contains specification required propagators that are actually hosted in this repo.
    // Any other propagators (like aws, aws-lambda, should go into `@opentelemetry/auto-configuration-propagators` instead).
    const propagatorsFactory = new Map([
        ['tracecontext', () => new core_1.W3CTraceContextPropagator()],
        ['baggage', () => new core_1.W3CBaggagePropagator()],
        ['b3', () => new propagator_b3_1.B3Propagator()],
        [
            'b3multi',
            () => new propagator_b3_1.B3Propagator({ injectEncoding: propagator_b3_1.B3InjectEncoding.MULTI_HEADER }),
        ],
        [
            'jaeger',
            () => {
                api_1.diag.warn('The Jaeger propagator is deprecated and will be removed in a future release. Use the W3C TraceContext propagator ("tracecontext") instead.');
                return new propagator_jaeger_1.JaegerPropagator();
            },
        ],
    ]);
    const propagators = [];
    for (const [name] of kvs) {
        const propagator = propagatorsFactory.get(name)?.();
        if (!propagator) {
            throw new Error(`unknown TextMapPropagator in configuration: "${name}"`);
        }
        propagators.push(propagator);
    }
    if (propagators.length === 0) {
        return undefined;
    }
    else {
        // Always wrap in a composite propagator, even for the rare case of a single
        // propagator, because `/propagator/composite` in the configuration schema
        // says "Configure the propagators in the composite text map propagator".
        return new core_1.CompositePropagator({ propagators });
    }
}
exports.createPropagatorFromConfig = createPropagatorFromConfig;
class ServiceNameDetector {
    detect() {
        const attributes = {};
        const serviceName = (0, core_1.getStringFromEnv)('OTEL_SERVICE_NAME');
        if (serviceName) {
            attributes[semantic_conventions_1.ATTR_SERVICE_NAME] = serviceName;
        }
        return { attributes };
    }
}
const serviceNameDetector = new ServiceNameDetector();
function createResourceFromConfig(resourceConfig) {
    // Limitation: Resource#merge() is the only exported mechanism from the
    // resources package that supports *async* attributes, so we must use it.
    // However, if `schemaUrl` is being used by detectors and in the config, then
    // `.merge()` will potentially warn and drop the schema URL if there is a
    // conflict.  (See `mergeSchemaUrl` behaviour.) This is likely not the
    // intended behavior when a user specifies a `schema_url` in the declarative
    // config file.
    checkConfigUse('Resource', resourceConfig, [
        'attributes',
        'attributes_list',
        'schema_url',
        'detection/development',
    ]);
    let resource = (0, resources_1.defaultResource)();
    if (!resourceConfig) {
        return resource;
    }
    if (resourceConfig['detection/development']) {
        // TODO(6986): support attributes.{include,exclude}; `resources` package doesn't currently support this
        checkConfigUse('ExperimentalResourceDetection', resourceConfig['detection/development'], ['detectors']);
        if (resourceConfig['detection/development'].detectors) {
            const detectors = [];
            for (const d of resourceConfig['detection/development'].detectors) {
                const [name] = mustSingleEntry(d, 'ExperimentalResourceDetector');
                // https://opentelemetry.io/docs/specs/otel-config/types/#type-experimentalresourcedetector
                switch (name) {
                    // Note: The 'container' detector defined in the schema cannot yet
                    // be supported because a container resource detector lives in the
                    // separate opentelemetry-js-contrib.git repo. Supporting 'container'
                    // will come as part of supporting PluginComponentProvider.
                    case 'host':
                        detectors.push(resources_1.hostDetector);
                        detectors.push(resources_1.osDetector);
                        break;
                    case 'process':
                        detectors.push(resources_1.processDetector);
                        break;
                    case 'service':
                        // Note: The declarative schema defines the 'service' detector to
                        // handle `service.instance.id` and the `OTEL_SERVICE_NAME` envvar.
                        // https://opentelemetry.io/docs/specs/otel-config/types/#type-experimentalresourcedetector
                        // This is equivalent to the `serviceInstanceIdDetector` and
                        // *part* of the `envDetector`.  Using this `envDetector` would
                        // incorrectly read the `OTEL_RESOURCE_ATTRIBUTES` envvar.
                        detectors.push(serviceNameDetector);
                        detectors.push(resources_1.serviceInstanceIdDetector);
                        break;
                    default:
                        throw new Error(`unknown ExperimentalResourceDetector name in configuration: "${name}"`);
                }
            }
            if (detectors.length > 0) {
                resource = resource.merge((0, resources_1.detectResources)({ detectors }));
            }
        }
    }
    const configAttrs = (0, configuration_1.mergeResourceAttributesConfig)(resourceConfig.attributes, resourceConfig.attributes_list);
    if (configAttrs && configAttrs.length > 0) {
        const attrs = {};
        for (const a of configAttrs) {
            if (a.value !== null) {
                attrs[a.name] = a.value;
            }
        }
        resource = resource.merge((0, resources_1.resourceFromAttributes)(attrs));
    }
    if (resourceConfig.schema_url) {
        resource = resource.merge((0, resources_1.resourceFromAttributes)({}, { schemaUrl: resourceConfig.schema_url }));
    }
    return resource;
}
exports.createResourceFromConfig = createResourceFromConfig;
function createLogRecordLimitsFromConfig(limits, attribute_limits) {
    if (!limits && !attribute_limits) {
        return undefined;
    }
    return {
        attributeValueLengthLimit: limits?.attribute_value_length_limit ??
            attribute_limits?.attribute_value_length_limit ??
            undefined,
        attributeCountLimit: limits?.attribute_count_limit ??
            attribute_limits?.attribute_count_limit ??
            undefined,
    };
}
exports.createLogRecordLimitsFromConfig = createLogRecordLimitsFromConfig;
function createLogRecordExporterFromConfig(exporter) {
    const [name, properties] = mustSingleEntry(exporter, 'LogRecordExporter');
    switch (name) {
        case 'otlp_http': {
            checkConfigUse('OtlpHttpExporter', properties, [
                'compression',
                'endpoint',
                'headers',
                'headers_list',
                'timeout',
                'tls',
                'encoding',
            ]);
            const props = properties;
            const commonOpts = {
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                headers: (0, configuration_1.mergeHeadersConfig)(props?.headers, props?.headers_list),
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'OtlpHttpExporter.timeout'),
                httpAgentOptions: httpTlsOptionsFromConfig(props?.tls),
            };
            const encoding = props?.encoding ?? 'protobuf';
            switch (encoding) {
                case 'json':
                    return new exporter_logs_otlp_http_1.OTLPLogExporter(commonOpts);
                case 'protobuf':
                    return new exporter_logs_otlp_proto_1.OTLPLogExporter(commonOpts);
                default:
                    throw new Error(`unknown OtlpHttpExporter encoding in configuration: "${encoding}"`);
            }
        }
        case 'otlp_grpc': {
            checkConfigUse('OtlpGrpcExporter', properties, [
                'compression',
                'endpoint',
                'timeout',
                'tls',
                'headers',
                'headers_list',
            ]);
            const props = properties;
            return new exporter_logs_otlp_grpc_1.OTLPLogExporter({
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'OtlpGrpcExporter.timeout'),
                credentials: grpcCredentialsFromConfig(props?.tls),
                metadata: getGrpcMetadataFromHeaders(props?.headers, props?.headers_list),
            });
        }
        case 'console':
            return new sdk_logs_1.ConsoleLogRecordExporter();
        default:
            throw new Error(`unknown LogRecordExporter name in configuration: "${name}"`);
    }
}
exports.createLogRecordExporterFromConfig = createLogRecordExporterFromConfig;
function createLogRecordProcessorFromConfig(processor) {
    const [name, properties] = mustSingleEntry(processor, 'LogRecordProcessor');
    switch (name) {
        case 'batch': {
            checkConfigUse('BatchLogRecordProcessor', properties, [
                'exporter',
                'max_queue_size',
                'max_export_batch_size',
                'schedule_delay',
                'export_timeout',
            ]);
            const props = properties;
            const exporter = createLogRecordExporterFromConfig(props.exporter);
            return new sdk_logs_1.BatchLogRecordProcessor({
                exporter,
                maxQueueSize: props.max_queue_size ?? undefined,
                maxExportBatchSize: props.max_export_batch_size ?? undefined,
                scheduledDelayMillis: props.schedule_delay ?? undefined,
                exportTimeoutMillis: validateExportTimeoutConfig(props.export_timeout, 'BatchLogRecordProcessor.export_timeout'),
            });
        }
        case 'simple': {
            const props = properties;
            const exporter = createLogRecordExporterFromConfig(props.exporter);
            return new sdk_logs_1.SimpleLogRecordProcessor({ exporter });
        }
        default:
            throw new Error(`unknown LogRecordProcessor name: "${name}"`);
    }
}
exports.createLogRecordProcessorFromConfig = createLogRecordProcessorFromConfig;
function createLoggerProviderFromConfig(resource, logger_provider, attribute_limits) {
    const processors = logger_provider.processors.map(p => createLogRecordProcessorFromConfig(p));
    const logRecordLimits = createLogRecordLimitsFromConfig(logger_provider.limits, attribute_limits);
    checkConfigUse('LoggerProvider', logger_provider, ['processors', 'limits']);
    return new sdk_logs_1.LoggerProvider({
        resource,
        processors,
        logRecordLimits,
        // TODO: loggerConfigurator
        // TODO: meterProvider
        // Note: forceFlushTimeoutMillis not configurable via decl conf.
    });
}
exports.createLoggerProviderFromConfig = createLoggerProviderFromConfig;
// Exported for testing.
function createSpanLimitsFromConfig(limits, attribute_limits) {
    if (!limits && !attribute_limits) {
        return undefined;
    }
    return {
        attributeValueLengthLimit: limits?.attribute_value_length_limit ??
            attribute_limits?.attribute_value_length_limit ??
            undefined,
        attributeCountLimit: limits?.attribute_count_limit ??
            attribute_limits?.attribute_count_limit ??
            undefined,
        eventCountLimit: limits?.event_count_limit ?? undefined,
        linkCountLimit: limits?.link_count_limit ?? undefined,
        attributePerEventCountLimit: limits?.event_attribute_count_limit ?? undefined,
        attributePerLinkCountLimit: limits?.link_attribute_count_limit ?? undefined,
    };
}
exports.createSpanLimitsFromConfig = createSpanLimitsFromConfig;
/**
 * Map a declarative-config `temporality_preference` value to the enum the OTLP
 * metric exporters expect. Returns undefined for an unspecified preference so
 * the exporter falls back to its own default (cumulative).
 */
function aggregationTemporalityPreferenceFromConfig(temporality_preference) {
    if (temporality_preference == null) {
        return undefined;
    }
    switch (temporality_preference) {
        case 'delta':
            return exporter_metrics_otlp_http_2.AggregationTemporalityPreference.DELTA;
        case 'low_memory':
            return exporter_metrics_otlp_http_2.AggregationTemporalityPreference.LOWMEMORY;
        case 'cumulative':
            return exporter_metrics_otlp_http_2.AggregationTemporalityPreference.CUMULATIVE;
        default:
            throw new Error(`unknown temporality_preference value in configuration: ${temporality_preference}`);
    }
}
/**
 * Map a declarative-config `default_histogram_aggregation` value to an
 * AggregationSelector that applies the requested aggregation to histogram
 * instruments and leaves all other instrument types at their default. Returns
 * undefined for an unspecified value so the exporter uses its own default.
 */
function aggregationSelectorFromConfig(default_histogram_aggregation) {
    if (default_histogram_aggregation == null) {
        return undefined;
    }
    let histoAggOpt;
    switch (default_histogram_aggregation) {
        case 'base2_exponential_bucket_histogram':
            histoAggOpt = { type: sdk_metrics_1.AggregationType.EXPONENTIAL_HISTOGRAM };
            break;
        case 'explicit_bucket_histogram':
            histoAggOpt = { type: sdk_metrics_1.AggregationType.EXPLICIT_BUCKET_HISTOGRAM };
            break;
        default:
            throw new Error(`unknown default_histogram_aggregation value in configuration: ${default_histogram_aggregation}`);
    }
    return (instrumentType) => instrumentType === sdk_metrics_1.InstrumentType.HISTOGRAM
        ? histoAggOpt
        : { type: sdk_metrics_1.AggregationType.DEFAULT };
}
/** exported for testing */
function createPushMetricExporterFromConfig(exporter) {
    const [name, properties] = mustSingleEntry(exporter, 'PushMetricExporter');
    // TODO(6959): use non-envvar reading mechanism to create OTLP exporters, so an `undefined` option does NOT fallback to an envvar
    switch (name) {
        case 'otlp_http': {
            checkConfigUse('PushMetricExporter', properties, [
                'compression',
                'endpoint',
                'headers',
                'headers_list',
                'timeout',
                'tls',
                'temporality_preference',
                'default_histogram_aggregation',
                'encoding',
            ]);
            const props = properties;
            const commonOpts = {
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                headers: (0, configuration_1.mergeHeadersConfig)(props?.headers, props?.headers_list),
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'PushMetricExporter.timeout'),
                httpAgentOptions: httpTlsOptionsFromConfig(props?.tls),
                temporalityPreference: aggregationTemporalityPreferenceFromConfig(props?.temporality_preference),
                aggregationPreference: aggregationSelectorFromConfig(props?.default_histogram_aggregation),
            };
            const encoding = props?.encoding ?? 'protobuf';
            switch (encoding) {
                case 'json':
                    return new exporter_metrics_otlp_http_1.OTLPMetricExporter(commonOpts);
                case 'protobuf':
                    return new exporter_metrics_otlp_proto_1.OTLPMetricExporter(commonOpts);
                default:
                    throw new Error(`unknown OtlpHttpMetricExporter encoding in configuration: "${encoding}"`);
            }
        }
        case 'otlp_grpc': {
            checkConfigUse('PushMetricExporter', properties, [
                'compression',
                'endpoint',
                'timeout',
                'tls',
                'headers',
                'headers_list',
                'temporality_preference',
                'default_histogram_aggregation',
            ]);
            const props = properties;
            return new exporter_metrics_otlp_grpc_1.OTLPMetricExporter({
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'PushMetricExporter.timeout'),
                credentials: grpcCredentialsFromConfig(props?.tls),
                metadata: getGrpcMetadataFromHeaders(props?.headers, props?.headers_list),
                temporalityPreference: aggregationTemporalityPreferenceFromConfig(props?.temporality_preference),
                aggregationPreference: aggregationSelectorFromConfig(props?.default_histogram_aggregation),
            });
        }
        case 'console': {
            // TODO(6957): temporality_preference
            // TODO(6957): default_histogram_aggregation
            checkConfigUse('PushMetricExporter', properties, []);
            return new sdk_metrics_1.ConsoleMetricExporter();
        }
        default:
            throw new Error(`unknown PushMetricExporter name in configuration: "${name}"`);
    }
}
exports.createPushMetricExporterFromConfig = createPushMetricExporterFromConfig;
function createMetricProducerFromConfig(producer) {
    const [name] = mustSingleEntry(producer, 'MetricProducer');
    switch (name) {
        case 'opencensus': {
            // Note: The "opencensus" MetricProducer is intentionally not supported.
            // It is deprecated in OpenTelemetry Configuration v1.2.0.
            throw new Error('the "opencensus" MetricProducer is deprecated and not supported');
        }
        default:
            throw new Error(`unknown MetricProducer name: "${name}"`);
    }
}
function createPeriodicMetricReaderFromConfig(periodic) {
    const exporter = createPushMetricExporterFromConfig(periodic.exporter);
    const metricProducers = periodic.producers?.map(createMetricProducerFromConfig);
    checkConfigUse('PeriodicMetricReader', periodic, [
        'exporter',
        'producers',
        'interval',
        'timeout',
        'max_export_batch_size/development',
    ]);
    // TODO(6425): add cardinality_limits
    return new sdk_metrics_1.PeriodicExportingMetricReader({
        exportIntervalMillis: periodic.interval ?? 60000,
        exportTimeoutMillis: validateExportTimeoutConfig(periodic?.timeout, 'PeriodicExportingMetricReader.timeout') ?? 30000,
        exporter,
        metricProducers,
        maxExportBatchSize: periodic['max_export_batch_size/development'] ?? undefined,
    });
}
function createPullMetricReaderFromConfig(pull) {
    // TODO(6425): add cardinality_limits
    checkConfigUse('PullMetricReader', pull, ['exporter', 'producers']);
    const metricProducers = pull.producers?.map(createMetricProducerFromConfig);
    // The declarative config models a `pull` MetricReader as an object that takes
    // an exporter, e.g.:
    //    meter_provider:
    //      readers:
    //        - pull:
    //            exporter:
    //              prometheus/development: ... # (A)
    //            # (B)
    //            producers: ...
    //            cardinality_limits: ...
    // but the exporter (or at least the only current one, PrometheusExporter)
    // *is* a MetricReader, and takes configuration from (A) and (B).
    //    export class PrometheusExporter extends MetricReader { ... }
    const [name, properties] = mustSingleEntry(pull.exporter, 'PullMetricExporter');
    switch (name) {
        case 'prometheus/development': {
            // TODO(6653): 'translation_strategy' with #6653 or separately
            // TODO(6605): 'resource_constant_labels' map to `withResourceConstantLabels` regex
            checkConfigUse('ExperimentalPrometheusMetricExporterConfigModel', properties, [
                'host',
                'port',
                'scope_info_enabled',
                'target_info_enabled/development',
            ]);
            const props = properties;
            return new exporter_prometheus_1.PrometheusExporter({
                // TODO(6605): default host to `undefined` when PrometheusExporter defaults to 'localhost'
                host: props?.host ?? 'localhost',
                // TODO(6958): default port and host to `undefined` when PrometheusExporter constructor no longer reads envvars for config
                port: props?.port ?? 9464,
                withoutScopeInfo: props?.scope_info_enabled != null
                    ? !props.scope_info_enabled
                    : undefined,
                withoutTargetInfo: props?.['target_info_enabled/development'] != null
                    ? !props['target_info_enabled/development']
                    : undefined,
                metricProducers,
                // Note: The following are not configurable via declarative config.
                // This isn't necessarily an issue.
                // - prefix
                // - appendTimestamp
                // - endpoint
                // - preventServerStart
                // - callback (second arg)
            });
        }
        default:
            throw new Error(`unknown PullMetricReader name in configuration: "${name}"`);
    }
}
function createMetricReaderFromConfig(reader) {
    const [name, properties] = mustSingleEntry(reader, 'MetricReader');
    switch (name) {
        case 'periodic':
            return createPeriodicMetricReaderFromConfig(properties);
        case 'pull':
            return createPullMetricReaderFromConfig(properties);
        default:
            throw new Error(`unknown MetricReader type in configuration: "${Object.keys(reader)[0]}"`);
    }
}
function instrumentTypeFromConfig(instrument_type) {
    switch (instrument_type) {
        case 'counter':
            return sdk_metrics_1.InstrumentType.COUNTER;
        case 'gauge':
            return sdk_metrics_1.InstrumentType.GAUGE;
        case 'histogram':
            return sdk_metrics_1.InstrumentType.HISTOGRAM;
        case 'observable_counter':
            return sdk_metrics_1.InstrumentType.OBSERVABLE_COUNTER;
        case 'observable_gauge':
            return sdk_metrics_1.InstrumentType.OBSERVABLE_GAUGE;
        case 'observable_up_down_counter':
            return sdk_metrics_1.InstrumentType.OBSERVABLE_UP_DOWN_COUNTER;
        case 'up_down_counter':
            return sdk_metrics_1.InstrumentType.UP_DOWN_COUNTER;
        default:
            throw new Error(`unknown InstrumentType: ${instrument_type}`);
    }
}
function createAggregationOptionFromConfig(aggregation) {
    const [name, properties] = mustSingleEntry(aggregation, 'Aggregation');
    switch (name) {
        case 'default':
            return {
                type: sdk_metrics_1.AggregationType.DEFAULT,
            };
        case 'drop':
            return {
                type: sdk_metrics_1.AggregationType.DROP,
            };
        case 'explicit_bucket_histogram': {
            checkConfigUse('ExplicitBucketHistogramAggregation', properties, [
                'record_min_max',
                'boundaries',
            ]);
            const props = properties;
            return {
                type: sdk_metrics_1.AggregationType.EXPLICIT_BUCKET_HISTOGRAM,
                options: {
                    recordMinMax: props?.record_min_max ?? true,
                    boundaries: props?.boundaries ?? [
                        0, 5, 10, 25, 50, 75, 100, 250, 500, 750, 1000, 2500, 5000, 7500,
                        10000,
                    ],
                },
            };
        }
        case 'base2_exponential_bucket_histogram': {
            // TODO(3841): max_scale property
            checkConfigUse('Base2ExponentialBucketHistogramAggregation', properties, ['record_min_max', 'max_size']);
            const props = properties;
            return {
                type: sdk_metrics_1.AggregationType.EXPONENTIAL_HISTOGRAM,
                options: {
                    recordMinMax: props?.record_min_max ?? undefined,
                    maxSize: props?.max_size ?? undefined,
                },
            };
        }
        case 'last_value':
            return {
                type: sdk_metrics_1.AggregationType.LAST_VALUE,
            };
        case 'sum':
            return {
                type: sdk_metrics_1.AggregationType.SUM,
            };
        default:
            throw new Error(`unknown Aggregation name in configuration: "${name}"`);
    }
}
function createViewOptionsFromConfig(view) {
    const viewOptions = {};
    checkConfigUse('View', view, ['selector', 'stream']);
    checkConfigUse('ViewSelector', view.selector, [
        'instrument_name',
        'instrument_type',
        'unit',
        'meter_name',
        'meter_version',
        'meter_schema_url',
    ]);
    if (view.selector.instrument_name) {
        viewOptions.instrumentName = view.selector.instrument_name;
    }
    if (view.selector.instrument_type) {
        viewOptions.instrumentType = instrumentTypeFromConfig(view.selector.instrument_type);
    }
    if (view.selector.unit) {
        viewOptions.instrumentUnit = view.selector.unit;
    }
    if (view.selector.meter_name) {
        viewOptions.meterName = view.selector.meter_name;
    }
    if (view.selector.meter_version) {
        viewOptions.meterVersion = view.selector.meter_version;
    }
    if (view.selector.meter_schema_url) {
        viewOptions.meterSchemaUrl = view.selector.meter_schema_url;
    }
    checkConfigUse('ViewStream', view.stream, [
        'name',
        'description',
        'aggregation',
        'aggregation_cardinality_limit',
        'attribute_keys',
    ]);
    if (view.stream.name) {
        viewOptions.name = view.stream.name;
    }
    if (view.stream.description) {
        viewOptions.description = view.stream.description;
    }
    if (view.stream.aggregation) {
        viewOptions.aggregation = createAggregationOptionFromConfig(view.stream.aggregation);
    }
    viewOptions.aggregationCardinalityLimit =
        view.stream.aggregation_cardinality_limit ?? undefined;
    if (view.stream.attribute_keys) {
        const processors = [];
        if (view.stream.attribute_keys.included &&
            view.stream.attribute_keys.included.length > 0) {
            // TODO(6951): support wildcards
            // Current sdk-metrics does not support *patterns* via '*' and '?'
            // as defined by IncludeExclude type in declarative config.
            const unsupportedKeys = view.stream.attribute_keys.included.filter(k => k.includes('*') || k.includes('?'));
            if (unsupportedKeys.length > 0) {
                throw new Error(`invalid /meter_provider/views/?/stream/attribute_keys/included in configuration: do not support wildcards: "${unsupportedKeys.join('", "')}"`);
            }
            processors.push((0, sdk_metrics_1.createAllowListAttributesProcessor)(view.stream.attribute_keys.included));
        }
        if (view.stream.attribute_keys.excluded &&
            view.stream.attribute_keys.excluded.length > 0) {
            // TODO(6951): support wildcards
            // Current sdk-metrics does not support *patterns* via '*' and '?'
            // as defined by IncludeExclude type in declarative config.
            const unsupportedKeys = view.stream.attribute_keys.excluded.filter(k => k.includes('*') || k.includes('?'));
            if (unsupportedKeys.length > 0) {
                throw new Error(`invalid /meter_provider/views/?/stream/attribute_keys/excluded in configuration: do not support wildcards: "${unsupportedKeys.join('", "')}"`);
            }
            processors.push((0, sdk_metrics_1.createDenyListAttributesProcessor)(view.stream.attribute_keys.excluded));
        }
        if (processors.length > 0) {
            viewOptions.attributesProcessors = processors;
        }
    }
    return viewOptions;
}
function createMeterProviderFromConfig(resource, meter_provider) {
    const readers = meter_provider.readers.map(createMetricReaderFromConfig);
    const views = meter_provider.views?.map(createViewOptionsFromConfig);
    // TODO(5147): exemplar_filter when sdk-metrics package supports it
    // TODO(6952): meter_configurator/development when sdk-metrics package supports it
    checkConfigUse('MeterProvider', meter_provider, ['readers', 'views']);
    return new sdk_metrics_1.MeterProvider({
        resource,
        readers,
        views,
        // Note: sdkMetricsEnabled not configurable via decl conf. Once SDK health metrics are stabilized, they will be on by default.
    });
}
exports.createMeterProviderFromConfig = createMeterProviderFromConfig;
function createSpanExporterFromConfig(exporter) {
    const [name, properties] = mustSingleEntry(exporter, 'SpanExporter');
    switch (name) {
        case 'otlp_http': {
            checkConfigUse('OtlpHttpExporter', properties, [
                'compression',
                'endpoint',
                'headers',
                'headers_list',
                'timeout',
                'tls',
                'encoding',
            ]);
            const props = properties;
            const commonOpts = {
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                headers: (0, configuration_1.mergeHeadersConfig)(props?.headers, props?.headers_list),
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'OtlpHttpExporter.timeout'),
                httpAgentOptions: httpTlsOptionsFromConfig(props?.tls),
            };
            const encoding = props?.encoding ?? 'protobuf';
            switch (encoding) {
                case 'json':
                    return new exporter_trace_otlp_http_1.OTLPTraceExporter(commonOpts);
                case 'protobuf':
                    return new exporter_trace_otlp_proto_1.OTLPTraceExporter(commonOpts);
                default:
                    throw new Error(`unknown OtlpHttpExporter encoding in configuration: "${encoding}"`);
            }
        }
        case 'otlp_grpc': {
            checkConfigUse('OtlpGrpcExporter', properties, [
                'compression',
                'endpoint',
                'timeout',
                'tls',
                'headers',
                'headers_list',
            ]);
            const props = properties;
            return new exporter_trace_otlp_grpc_1.OTLPTraceExporter({
                compression: props?.compression === 'gzip'
                    ? otlp_exporter_base_1.CompressionAlgorithm.GZIP
                    : otlp_exporter_base_1.CompressionAlgorithm.NONE,
                url: props?.endpoint ?? undefined,
                timeoutMillis: validateExportTimeoutConfig(props?.timeout, 'OtlpGrpcExporter.timeout'),
                credentials: grpcCredentialsFromConfig(props?.tls),
                metadata: getGrpcMetadataFromHeaders(props?.headers, props?.headers_list),
            });
        }
        case 'console':
            return new sdk_trace_1.ConsoleSpanExporter();
        default:
            throw new Error(`unknown SpanExporter name in configuration: "${name}"`);
    }
}
function createSpanProcessorFromConfig(processor) {
    const [name, properties] = mustSingleEntry(processor, 'SpanProcessor');
    switch (name) {
        case 'batch': {
            checkConfigUse('BatchSpanProcessor', properties, [
                'exporter',
                'max_queue_size',
                'max_export_batch_size',
                'schedule_delay',
                'export_timeout',
            ]);
            const props = properties;
            const exporter = createSpanExporterFromConfig(props.exporter);
            return new sdk_trace_1.BatchSpanProcessor({
                exporter,
                maxQueueSize: props.max_queue_size ?? undefined,
                maxExportBatchSize: props.max_export_batch_size ?? undefined,
                scheduledDelayMillis: props.schedule_delay ?? undefined,
                exportTimeoutMillis: validateExportTimeoutConfig(props.export_timeout, 'BatchSpanProcessor.export_timeout'),
            });
        }
        case 'simple': {
            const props = properties;
            const exporter = createSpanExporterFromConfig(props.exporter);
            return new sdk_trace_1.SimpleSpanProcessor({ exporter });
        }
        default:
            throw new Error(`unknown SpanProcessor name: "${name}"`);
    }
}
/**
 * Returns a Sampler for the `tracer_provider.sampler` in configuration,
 * or `undefined` if not set.
 *
 * Exported for testing.
 */
function createSamplerFromConfig(samplerConfig) {
    if (!samplerConfig) {
        return undefined;
    }
    const [name, properties] = mustSingleEntry(samplerConfig, 'Sampler');
    switch (name) {
        case 'always_off':
            return new sdk_trace_1.AlwaysOffSampler();
        case 'always_on':
            return new sdk_trace_1.AlwaysOnSampler();
        case 'trace_id_ratio_based': {
            const DEFAULT_RATIO = 1.0;
            const props = properties;
            return new sdk_trace_1.TraceIdRatioBasedSampler(props?.ratio ?? DEFAULT_RATIO);
        }
        case 'parent_based': {
            const props = properties;
            return new sdk_trace_1.ParentBasedSampler({
                root: createSamplerFromConfig(props?.root) ?? new sdk_trace_1.AlwaysOnSampler(),
                remoteParentSampled: createSamplerFromConfig(props?.remote_parent_sampled),
                remoteParentNotSampled: createSamplerFromConfig(props?.remote_parent_not_sampled),
                localParentSampled: createSamplerFromConfig(props?.local_parent_sampled),
                localParentNotSampled: createSamplerFromConfig(props?.local_parent_not_sampled),
            });
        }
        // TODO: always_record, once there is a release of opentelemetry-configuration with https://github.com/open-telemetry/opentelemetry-configuration/pull/698
        // case 'always_record': {
        //   const props = properties as AlwaysRecordConfigModel;
        //   return createAlwaysRecordSampler(createSamplerFromConfig(props.root));
        // }
        // TODO(6961): composite/development
        // TODO(6541): probability/development (via composite support for?)
        // TODO(later): Consider supporting jaeger_remote/development if requested by a user. Only consider `sampler-jaeger-remote` package after axios dep is dropped in #6963.
        default:
            throw new Error(`unknown Sampler name: "${name}"`);
    }
}
exports.createSamplerFromConfig = createSamplerFromConfig;
function createIdGeneratorFromConfig(id_generator) {
    if (!id_generator) {
        return undefined;
    }
    const [name] = mustSingleEntry(id_generator, 'IdGenerator');
    switch (name) {
        case 'random':
            return new sdk_trace_1.RandomIdGenerator();
        default:
            throw new Error(`unknown IdGenerator name: "${name}"`);
    }
}
exports.createIdGeneratorFromConfig = createIdGeneratorFromConfig;
function createTracerProviderFromConfig(resource, tracer_provider, attribute_limits) {
    const spanProcessors = tracer_provider.processors.map(p => createSpanProcessorFromConfig(p));
    const spanLimits = createSpanLimitsFromConfig(tracer_provider.limits, attribute_limits);
    const sampler = createSamplerFromConfig(tracer_provider.sampler);
    const idGenerator = createIdGeneratorFromConfig(tracer_provider.id_generator);
    checkConfigUse('TracerProvider', tracer_provider, [
        'processors',
        'limits',
        'sampler',
        'id_generator',
    ]);
    // TODO(6960): 'tracer_configurator/development', TracerProvider doesn't currently support this
    // TODO(6624): meterProvider, if SDK health metrics enabled
    return new sdk_trace_1.TracerProvider({
        resource,
        spanProcessors,
        spanLimits,
        sampler,
        idGenerator,
    });
}
exports.createTracerProviderFromConfig = createTracerProviderFromConfig;
//# sourceMappingURL=create-from-config.js.map