"use strict";
/*
 * Copyright The OpenTelemetry Authors
 * SPDX-License-Identifier: Apache-2.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.once = exports.getSpanName = exports.getOperationName = void 0;
/**
 * Returns the `db.operation.name` value for a given tedious method.
 *
 * - `callProcedure` → `"EXECUTE"`
 * - `execBulkLoad`  → `"BULK INSERT"`
 * - everything else → the method name as-is (e.g. `"execSql"`, `"prepare"`)
 */
function getOperationName(tediousMethod) {
    if (tediousMethod === 'callProcedure')
        return 'EXECUTE';
    if (tediousMethod === 'execBulkLoad')
        return 'BULK INSERT';
    return tediousMethod;
}
exports.getOperationName = getOperationName;
/**
 *   {db.operation.name} {target}
 *   {db.operation.name}
 *   {target}
 *   {db.system.name}
 *
 * where {target} resolves to db.collection.name first, then db.namespace.
 */
function getSpanName(operationName, db, collection, dbSystemName) {
    // {target} = db.collection.name preferred over db.namespace
    const target = collection ?? db;
    if (operationName && target)
        return `${operationName} ${target}`;
    if (operationName)
        return operationName;
    if (target)
        return target;
    return dbSystemName;
}
exports.getSpanName = getSpanName;
const once = (fn) => {
    let called = false;
    return (...args) => {
        if (called)
            return;
        called = true;
        return fn(...args);
    };
};
exports.once = once;
//# sourceMappingURL=utils.js.map