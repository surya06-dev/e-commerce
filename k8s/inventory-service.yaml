apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: microservices
  labels: { app: inventory-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: inventory-service }
  template:
    metadata:
      labels: { app: inventory-service }
    spec:
      imagePullSecrets:
        - name: ghcr-creds # only needed if your GHCR images/packages are private -- see README
      containers:
        - name: inventory-service
          image: ${IMAGE_BASE}/inventory-service:${IMAGE_TAG}
          ports:
            - containerPort: 4005
          env:
            - name: PORT
              value: "4005"
            - name: MONGO_URI
              value: "mongodb://mongo:27017/inventory_db"
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "250m", memory: "256Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 4005 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 4005 }
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: microservices
spec:
  selector: { app: inventory-service }
  ports:
    - port: 4005
      targetPort: 4005
